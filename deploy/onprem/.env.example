# 고객사가 이 파일을 복사해 .env로 만들고 실제 값을 채웁니다.
# cp .env.example .env

# 프론트엔드 런타임 config
API_BASE_URL=http://hub-api:8080
AUTH_BASE_URL=http://hub-api:8080/auth
DEPLOY_TARGET=onprem
ENABLE_HUB=true

# DB (TODO: 실제 백엔드 연결 시 채울 것)
DB_PASSWORD=changeme
DB_NAME=hubdb
